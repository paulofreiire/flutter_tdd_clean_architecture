name: flutter_tdd_clean
description: Projeto do curso Flutter TDD Clean Architecture Manguinho.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  http:
  provider:
  equatable:
  get:

dev_dependencies:
  flutter_test:
    sdk: flutter
  test:
  mockito:
  faker:

flutter:
  uses-material-design: true
  assets:
    - lib/ui/assets/